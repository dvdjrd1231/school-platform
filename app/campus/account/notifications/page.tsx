import { UpdatesFeed } from "@/components/notifications/updates-feed"

/** The campus-side view of the same notification feed as /updates. */
export default function CampusNotificationsPage() {
  return <UpdatesFeed title="Notifications" />
}
